/*
 * Copyright (C) 2016 Nikolay Dimitrov <picmaster@mail.bg>
 *
 * SPDX-License-Identifier: BSD-3-Clause
 */

#include "resources.h"
//#include "resource_data.h"

#include <stdio.h>

// FIXME: Use the bin-strings from resource_data.h
/*static uint8_t string1[] =
{
    0x01, 0x02, 0x03, 0x04
};

static uint8_t string2[] =
{
    0x05, 0x06, 0x07, 0x08
};

static uint8_t font_fixed8[] =
{
    0xDE, 0xAD, 0xC0, 0xDE
};

static uint8_t font_fixed10[] =
{
    0xCA, 0xFE, 0xBA, 0xBE
};

static uint8_t image1[] =
{
    42, 42, 42, 42
};

static uint8_t image2[] =
{
    42, 42, 42, 42
};

static resource_t resources[] =
{
    {.ptr = NULL, .len = 0},                             // RES_ID_INVALID
    {.ptr = string1, .len = sizeof(string1)},           // RES_ID_STRING1
    {.ptr = string2, .len = sizeof(string2)},           // RES_ID_STRING2
    {.ptr = font_fixed8, .len = sizeof(font_fixed8)},   // RES_ID_FONT_FIXED8
    {.ptr = font_fixed10, .len = sizeof(font_fixed10)}, // RES_ID_FONT_FIXED10
    {.ptr = image1, .len = sizeof(image1)},             // RES_ID_IMAGE_1
    {.ptr = image2, .len = sizeof(image2)}              // RES_ID_IMAGE_2
};

resource_t get_resource(resource_id_t id)
{
    if ((id > RES_ID_INVALID) && (id < RES_ID_MAX))
        return resources[id];
    else return resources[RES_ID_INVALID];
}*/

