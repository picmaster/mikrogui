mg_text_t ${form}_text${id} =
{
    .widget =
    {
        .type = MG_WIDGET_TYPE_TEXT,
        .geometry =
        {
            .x = ${x},
            .y = ${y},
            .w = ${w},
            .h = ${h}
        }
    },
    .font = ${font},
    .string = "${string}"
};
